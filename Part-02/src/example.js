// (1) Using the prettier.io sandbox we can design a set of rules that better adjust to our code style
// Then we can export those rules into a .prettierrc file
const foo = 'Freddy'
typeof foo === 'string'

if (!('serviceWorker' in navigator)) {
   // you have an old browser :-(
}

const greeting = 'hello'
console.log(`${greeting} world!`)
;[(1, 2, 3)].forEach(x => console.log(x))
